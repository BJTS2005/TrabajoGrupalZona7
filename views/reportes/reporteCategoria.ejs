<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <%- include ../../views/layout/cabecera.ejs %>

    <div class="container mt-5">
        

        <h1 class="text-center mt-4">Reporte de Categoría: <%= categoria.nombre %>
        </h1>
        <div class="mt-4">
            <p><strong>Total de Indicadores:</strong>
                <%= categoria.totalIndicadores %>
            </p>
            <p><strong>Puntaje Máximo:</strong>
                <%= categoria.puntajeMaximo %>
            </p>
            <p><strong>Puntaje Obtenido:</strong>
                <%= categoria.puntajeObtenido %>
            </p>
        </div>

        <% indicadores.forEach(indicador=> { %>
            <div class="card mt-4">
                <div class="card-header">
                    <strong>
                        <%= indicador.codigo %>
                    </strong>: <%= indicador.nombre %>
                </div>
                <div class="card-body">
                    <p><strong>Puntaje Máximo:</strong>
                        <%= indicador.puntajeMaximo %>
                    </p>
                    <p><strong>Puntaje Obtenido:</strong>
                        <%= indicador.puntajeObtenido %>
                    </p>
                    <h6 class="mt-3">Rangos Asociados</h6>
                    <table class="table table-bordered">
                        <thead class="table-dark">
                            <tr>
                                <th>Descripción</th>
                                <th>Valor (%)</th>
                                <th>Seleccionado</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (indicador.rangos.length> 0) { %>
                                <% indicador.rangos.forEach(rango=> { %>
                                    <tr class="<%= rango.esta_seleccionado ? 'table-success' : '' %>">
                                        <td>
                                            <%= rango.descripcion_ran %>
                                        </td>
                                        <td>
                                            <%= rango.valor_ran %>%
                                        </td>
                                        <td>
                                            <% if (rango.esta_seleccionado) { %>
                                                <span class="badge bg-success">Seleccionado</span>
                                                <% } else { %>
                                                    <span class="text-muted">-</span>
                                                    <% } %>
                                        </td>
                                    </tr>
                                    <% }); %>
                                        <% } else { %>
                                            <tr>
                                                <td colspan="3" class="text-center text-muted">Sin rangos asociados</td>
                                            </tr>
                                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
            <% }); %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>