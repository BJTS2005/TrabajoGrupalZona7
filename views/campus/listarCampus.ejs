<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        .campus-card {
            background-color: #005f73;
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            align-items: center;
            width: 200px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
    
        .campus-icon {
            font-size: 40px;
            color: white;
        }
    
        .campus-name {
            font-size: 16px;
            font-weight: bold;
            margin: 10px 0;
            text-align: center;
        }
    
     
    
        
    
        
    </style>
</head>

<body>
    <div class="container mt-4">
        <h1 class="text-center mb-4"><%= title %></h1>
        <div class="d-flex flex-wrap justify-content-center">
            <% datos.forEach(campus => { %>
                <div class="campus-card p-3 m-2">
                    <div class="text-center">
                        <i class="bi bi-building campus-icon mb-2"></i>
                        <p class="campus-name"><%= campus.camp_nom %></p>
                    </div>
                    <div class="d-flex flex-column mt-3"> <!-- Cambiado a flex-column -->
                        <a href="/campus/<%= campus.camp_id %>" class="btn btn-info btn-sm mb-2">Ver detalles</a>
                        <a href="/campus/editar/<%= campus.camp_id %>" class="btn btn-warning btn-sm mb-2">Editar</a>
                        <form action="/campus/eliminar/<%= campus.camp_id %>" method="POST" onsubmit="return confirm('¿Estás seguro de eliminar este campus?');">
                            <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                        </form>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
    


    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>