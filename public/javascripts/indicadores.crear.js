const opcBtn = document.getElementById('opcion-btn');
const subBtn = document.getElementById('sub-btn');
const opcContainer = document.getElementById('opciones-container');
const subContainer = document.getElementById('subComponente-container');

opcBtn.addEventListener('click', () => {
    
});